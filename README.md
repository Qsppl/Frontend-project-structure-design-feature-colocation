# Project design: colocation with features

Этот документ описывает дизайн структуры исходного кода web-проекта основанного на принципах группировки исходного кода по принципам принадлежности и со-локации

<br/>
<br/>
<br/>
<br/>
<br/>

# 🔥 Краткий обзор

## ❌ Так нельзя:

```
/src/
  /utils/           <- "Мусорка" (запрещено)
  /components/      <- Неясно, к чему относится (запрещено)
  login/            <- Где страница, где фича? (запрещено)
```

## ✅ Так правильно:

```
/                                # webroot
├── page.login/                  # Страница входа
│   ├── login-page.component.ts  # Компонент страницы
│   └── feature.auth-form/       # Фича формы
│       ├── auth-form.component.ts
│       ├── auth-form.component.spec.ts
│       └── auth-form.store.ts
│
├── (analytics)/                 # Логическая группа страниц аналитики
│ ├── page.dashboard/            # Подстраница группы
│ ├── page.reports/              # Подстраница группы
│ └── feature.charts/            # Общая фича для всей группы
│   ├── charts.component.ts
│   └── charts.service.ts
│
├── page.products/               # Страница товаров
│   ├── [id]/                    # Динамический роут
│   │   └── products-page.component.ts
│   └── feature.product-card/
│       ├── product-card.component.ts
│       └── internal.helpers/    # Приватные хелперы
│           ├── price.helper.ts  # 1 файл = 1 функция
│           └── discount.helper.ts
│
└── shared.features/             # Общие фичи
    └── feature.notifications/   # Уведомления
        ├── notify.component.ts
        └── notify.service.ts
```

## 💡 Суть:

1. `page.*/` — страницы (например, `page.dashboard/`).

1. `feature.*/` — фичи (например, `feature.search/`).

1. `internal.*/` — только для своей страницы/фичи.

1. `shared.*/` — для ресурсов разделенных между вложенными фичами/страницами.

1. Тесты лежат рядом с кодом (`*.spec.ts`).

### Особый случай: Группировка страниц

Используйте `(group)/` когда страницы:

- Принадлежат одному функциональному разделу
- Используют общие фичи
- Требуют изоляции от других частей приложения

## 🚫 Запрещено:

- Файлы `utils.ts`, `helpers.ts` (только `*.helper.ts`).

- Общие папки `/components/`, `/ui-kit/`.

- Импорт `internal.*` между разными страницами.

## 💡 Советы по организации

1. Тесты храните рядом с кодом:

    - `product-card.component.ts` → `product-card.component.spec.ts`

2. Вспомогательные функции – по одной в файле:

    - `/helpers/format-price.helper.ts`


## 🚀 Теперь вы знаете, как хранить файлы!

Следуйте этим правилам — и проект останется чистым даже через год.

<br/>
<br/>
<br/>
<br/>
<br/>

# 📚 Детальный разбор архитектуры: Эволюционный подход

Документ описывает поэтапное формирование архитектуры проекта, объясняя каждое решение и его преимущества.

## 🔹 Шаг 1: Feature-Based Группировка (Основной принцип)

> Смотри референсы в документации `Angular`/`Nest.js`

### Проблема:

Классическое разделение по типам файлов (`/components/`, `/services/`) приводит к:

- **"Мусорным ящикам"** с сотнями файлов
- **Неявным зависимостям** между модулями
- **Сложному удалению кода** (фича "размазана" по разным папкам).

### Решение:

Группировка по **фичам** (feature) — самостоятельным функциональным модулям.

**Пример:**

```
/cart/                  # Вся логика корзины в одном месте
  ├── Cart.js           # Компонент
  ├── cartStore.js      # Состояние
  └── cartService.js    # API-запросы
```

**Плюсы:**

- Все связанные файлы находятся в одной папке
- Чёткие границы модулей
- Упрощенный рефакторинг и удаление

## 🔹 Шаг 2: Page-Centric Группировка

> Смотри референсы в документации `Next.js`

### Проблема:

Фичи могут принадлежать:

- Конкретной странице (например, форма входа)
- Нескольким страницам (например, нотификации)
- Быть глобальными (например, корзина)

Следовательно **нужна дополнительная группировка**.

### Решение:

Вводим **страницы** как отдельные сущности.

**Пример:**

```
/login/               # Страница входа
  /auth-form/           # Фича формы (только для /login)
/products/            # Страница товаров
  /notifications/       # Фича уведомлений (общая для /products/, /products/discounts/ и /products/[id])
  /discounts/           # Страница товаров со скидкой
  /[id]/                # Динамическая страница товара
    /product-card/        # Фича (только для /products/[id])
/cart/                  # Глобальная фича
```

### Когда фича должна оставаться внутри страницы?

```
/page.products/
  └── feature.product-filters/  # Фича используется ТОЛЬКО здесь
```

**Критерии:**

- Используется только в одной странице
- Содержит логику, специфичную для этой страницы
- Имеет тесную связь с компонентами страницы

### Когда и как выносить фичу наружу?

1. Фича нужна в нескольких страницах

    ```
    # Переносим в ближайшую общую страницу:
    - /(catalog)/page.products/page.discounts/feature.reviews/
    + /(catalog)/page.products/feature.reviews/

    # Теперь доступно для:
    # /(catalog)/page.products/page.discounts/
    # /(catalog)/page.products/[id]/
    ```

2. Фича нужна в нескольких страницах одной группы

    ```
    # Переносим в общую зону группы:
    - /(catalog)/page.products/feature.reviews/

    # Теперь доступно для:
    # /(catalog)/page.products/
    # /(catalog)/page.categories/
    ```

3. Фича нужна в разных частях сайта

    ```
    # Переносим в глобальные shared-фичи:
    - /(catalog)/shared.features/feature.reviews/
    + /shared.features/features/feature.reviews/  # Теперь доступно везде
    ```

### Четкий алгоритм выноса

1. **Проверяем зависимости**

    ```
    // Было (привязано к блогу):
    interface Comment {
        blogPostId: string;  // ❌ Плохо
    }

    // Стало (универсально):
    interface Comment {
        targetId: string;    // ✅ Хорошо
        targetType: 'blog' | 'news';
    }
    ```

2. **Переносим файлы**

3. **Обновляем импорты**

### Контрольные вопросы перед выносом

1. **Фича действительно переиспользуется?**

    - Проверьте минимум 2 реальных использования вне исходной страницы.

2. **Нет ли привязки к контексту страницы?**

    - Уберите все упоминания page.products внутри фичи.

3. **Готова ли документация?**
    - Добавьте примеры использования в `/shared.features/feature.reviews`.

### Главные правила

1. **Не выносите "на будущее"** — только при реальной необходимости

2. **Сначала используйте `shared.*` внутри ближайшей общей группы** — и только потом глобально

3. **Тестируйте изолированность** — фича должна работать без исходной страницы

Такой подход гарантирует:

- Четкие критерии для выноса

- Безопасный перенос без поломок

- Удобное масштабирование архитектуры

## 🔹 Шаг 3: Префиксы `page.*` и `feature.*` (фикс шага 1 и шага 2)

> Смотри референсы в документации `Angular`

### Проблема:

Нет четкого различия между **страницами** и **фичами**.

Следовательно **нужны префиксы**.

### Решение:

```
/page.login/            # Страница входа
  /feature.auth-form/   # Фича формы (только для login)
/page.products/
  /[id]/                # Динамическая страница
    /feature.card/      # Фича карточки товара
/feature.cart/          # Глобальная фича корзины
```

✅ Теперь видно, что является **страницей**, а что — **фичей**;

## 🔹 Шаг 4: Логические группы через `(group)/`

> Смотри референсы в документации `Next.js`

### Проблема:

Некоторые страницы неправильно выражены в роутинге - не имеют общего корня, хотя на самом деле являются одним разделом сайта, например:

```
/feature.tables/
/feature.charts/
/page.sectors/
/page.districs/
/page.countries/
/page.analytic/
```

### Решение:

Группируем их через `(group)/`:

```
/(analytic)/            # Раздел аналитики
    /feature.tables/    # Фича таблиц используемая во многих страницах аналитики
    /feature.charts/    # Фича графиков используемая на всех страницах аналитики
    /page.sectors/
    /page.districs/
    /page.countries/
    /page.analytic/
```

**Плюсы:**

✅ Фичи аналитики не засоряют глобальный раздел фич сайта

✅ Четкая структура проекта.

## 🔹 Шаг 5: Единый принцип организации файлов (SRP + сортировки) (критерий организации файлов внутри групп)

> Смотри референсы в документации `Angular`

### Проблема:

Группировки (`page.*`, `feature.*`) решили проблему **принадлежности** (что к чему относится), но внутри фич/страниц файлы всё ещё **организуются** хаотично:

```
/page.products/
  ├── utils.ts
  ├── components.ts
  ├── auth.ts
  ├── shit.ts
  └── api/
```

Это мешает воспринимать структуру фичи/страницы.

### Решение:

1. разделить исходный код на **SRP-файлы**;

2. ввести **сортировки** по типам файлов.

> **Сортировка** — это **вторичный критерий** организации файлов **внутри уже сгруппированных сущностей** (страниц/фич).

используйте шаблон `[фича].[тип].[расширение]`
Токен `[фича]` позволяет выделить ответственность по SRP, а токен `[тип]` возволяет выполнить сортировки.

**Пример:**

- `cart.component.ts` — компонент корзины;
- `auth.guard.ts` — guard для авторизации.

**Типы файлов:**

| Тип              | Пример                      | Ответственность           |
| ---------------- | --------------------------- | ------------------------- |
| `*.component.*`  | `search.component.ts`       | UI + логика компонента    |
| `*.spec.*`       | `search.component.spec.ts`  | Тесты                     |
| `*.view.*`       | `line-chart.view.ts`        | UI                        |
| `*.view-model.*` | `line-chart.view-model.ts`  | Логика компонента         |
| `*.service.*`    | `api.service.ts`            | API-запросы               |
| `*.helper.*`     | `format-price.helper.ts`    | Чистая функция            |
| `*.entity.*`     | `line-chart.entity.ts`      | Модель/интерфейс/тип      |
| `*.collection.*` | `line-charts.collection.ts` | Коллекция моделей         |
| `*.store.*`      | `cart.store.ts`             | Состояние (Pinia/Hybrids) |

Структура папок внутри фичи:

```
/feature.cart/
├── cart.component.ts # Компонент
├── cart.store.ts # Хранилище
├── cart.service.ts # API
└── helpers/ # Хелперы (доп. уровень сортировки)
  ├── price.helper.ts # 1 файл = 1 функция
  └── discount.helper.ts
```

---

### Как сортировки работают с группировками?

1. **Сначала группировка** (определяем контекст):

    - `page.products/` — это страница;
    - `feature.cart/` — это фича.

2. **Затем сортировка** (организуем файлы внутри):

    - Для `page.products/`:

    ```
    /page.products/
    ├── products.page.ts       # Главный компонент страницы
    ├── products.api.ts        # API-запросы
    └── feature.product-list/  # Вложенная фича
        ├── product-list.component.ts
        └── product-list.store.ts
    ```

    - Для feature.cart/:

    ```
    /feature.cart/
    ├── cart.component.ts
    ├── cart.store.ts
    └── helpers/
        ├── price.helper.ts
        └── tax.helper.ts
    ```

### Зачем это нужно?

1. Предсказуемость:

    - По имени файла сразу ясно, что он делает (_.service._ — это API).

1. Масштабируемость:

    - Новые файлы добавляются по четким правилам, а не в "удобную" папку.

1. Безопасность рефакторинга:
    - Легко найти все файлы одного типа (например, все \*.store.ts).

## 🔹 Шаг 6: Контроль доступа (`internal.*`, `shared.*`)

> Смотри референсы в документации `Angular`

### Проблема:

Нужно ограничивать видимость модулей:

- Приватные фичи (только для своей страницы);
- Общие фичи (для подстраниц);
- Глобальные (для всех).

### Решение:

Вводим модификаторы:

```
/page.products/
  ├── internal.features/    # Только для products
  │   └── product-editor/   # Нельзя использовать в cart
  └── shared.features/      # Для подстраниц (products/[id])
    └── product-gallery/    # Можно в products/[id]
/shared.features/           # Глобальные фичи (например, cart)
```

Правила:

- `internal.*` — только для своей страницы/фичи;

- `shared.*` — для своей страницы/фичи и вложенных страни/фич;

## Заключение

Предложенная архитектура решает ключевые проблемы:

1. **Масштабируемость** через четкие границы модулей
2. **Поддерживаемость** благодаря явной структуре
3. **Безопасность** через контроль зависимостей
